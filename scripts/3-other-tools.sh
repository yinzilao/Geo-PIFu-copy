#!/bin/bash

sudo apt-get update

sudo snap install --classic code # or code-insiders

sudo apt-get install ristretto # image viewer

# conda colmap install
conda install conda-forge::colormap

#sudo apt update && sudo apt install snapd

